logging:
  file:
    path: E:/work/tmp/logs/consumer

spring:
  main:
    allow-bean-definition-overriding: true
  jackson:
    default-property-inclusion: non_null
  config:
    import:
      - nacos:${spring.application.name}.yaml?refreshEnabled=false
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        log-name: naming-${spring.application.name}
      config:
        namespace: 35
    sentinel:
      log:
        dir: ${logging.file.path}


houtu:
  data:
    security:
      secretKey: 881100bb332299eeaa88cc66aaeebb99