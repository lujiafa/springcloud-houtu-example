logging:
  file:
    path: E:/work/tmp/logs/provider

spring:
  config:
    import:
      - nacos:${spring.application.name}.yaml?refreshEnabled=false
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        log-name: naming-${spring.application.name}
      config:
        namespace: 35
